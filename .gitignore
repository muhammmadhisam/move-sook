# deps
node_modules
.pnpm-store

# build
dist
.next
out
.turbo

# env
.env
.env.local
.env.*.local

# logs
*.log
npm-debug.log*
pnpm-debug.log*

# os / editor
.DS_Store
*.tsbuildinfo

# prisma — generated client (regenerated by `db:generate` / Docker build)
packages/db/prisma/*.db
packages/db/src/generated/

# uploaded images (local dev store)
apps/api/uploads/
